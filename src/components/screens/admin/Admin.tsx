import React from 'react';
import { Text, View } from 'react-native';

const Admin: React.FC = () => {
    return (
        <View>
            <Text>Admin</Text>
        </View>
    );
};

export default Admin;
